spring:
  kafka:
    bootstrap-servers: localhost:9092
  datasource:
    url: jdbc:postgresql://postgres:5432/scheduler
    username: scheduler
    password: scheduler

  jpa:
    hibernate:
      ddl-auto: update

server:
  port: 8081